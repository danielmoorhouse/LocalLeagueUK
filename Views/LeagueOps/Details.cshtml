@model LocalLeague1.Models.LeagueOps

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Details</title>
</head>
<body>
    <div>
        <h4>LeagueOps</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.LeagueOpsName)
            </dt>
    
            <dd>
                @Html.DisplayFor(model => model.LeagueOpsName)
            </dd>
    
        </dl>
    </div>
    <p>
        @Html.ActionLink("Edit", "Edit", new { id = Model.LeagueOpsId }) |
        @Html.ActionLink("Back to List", "Index")
    </p>
</body>
</html>
